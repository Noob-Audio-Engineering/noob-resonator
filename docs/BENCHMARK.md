# Noob Resonator: measured

Generated by `cargo run --release --bin benchmark`. Every row names the figure, where it comes from, what this engine measures and whether the two agree.

**What is not here, and will not be until somebody measures it.** No row compares this plug-in with the device it answers, because nobody has measured that device — not this project, not the survey behind it, not any third party I could find, and it cannot be loaded outside its host. This document says what floor we reach. It does not say by how much we beat anybody, and it will not until a bench session produces the other number.

**And no row asserts a figure against the code that produced it.** The partial series are checked against Leissa, Abramowitz and Stegun, Russell and Lehtonen in `src/dsp/tests.rs`, and against an out-of-tree probe that implements Bessel functions from their integral representation and beam eigenvalues by bisection and has never seen this repository. What this file measures is the audio: the frequencies that come out, the decays that come out, and what they cost.

## Conditions

| | |
|---|---|
| sample rate | 48000 Hz |
| internal block | 128 samples |
| register block | 16 modes |
| mode capacity | 4096 per voice |
| search budget | 8192 candidates per block |
| transform | Blackman–Harris, four-times zero pad |
| contact points | 0.107 / 0.213 of the object, so no null lands on a partial by accident |
| vector width the build was given | **SSE2 only** — the portable baseline a shipped plug-in gets, which is what these              figures are for. `RUSTFLAGS=-C target-cpu=native` roughly halves them on a machine              with AVX-512, and produces a binary that will not start on one without it |
| build | release |

## Summary

| section | meets | misses | no published figure |
|---|---|---|---|
| The partial series, measured out of the audio | 8 | 0 | 0 |
| Tuning and decay, one mode at a time | 12 | 0 | 4 |
| Storing the decrement rather than the pole radius | 5 | 0 | 0 |
| Which modes, against a full-bank reference | 0 | 0 | 9 |
| The air columns | 3 | 0 | 2 |
| The statistical tail | 4 | 0 | 3 |
| Latency and the limiter | 4 | 0 | 0 |
| How long the mode search takes to settle | 9 | 0 | 0 |
| What a mode costs | 0 | 0 | 20 |
| **all** | **45** | **0** | **38** |

## The partial series, measured out of the audio

The object is struck once, the tail is transformed with a Blackman–Harris window and a four-times zero pad, and the peaks are picked. Every peak is then matched to the nearest frequency the object's own eigenvalue problem puts a partial at, and the row reports the **worst** of those distances.

The reference is the series `src/dsp/tests.rs` checks against Leissa, Abramowitz and Stegun, Russell and Lehtonen, and that an out-of-tree probe reproduces to under a ten-thousandth of a cent. **So this measures the whole chain from the eigenvalue to the loudspeaker, not the arithmetic against itself.**

Matching rather than counting, because which partials a strike excites is physics and not a fault. A surface's degenerate pairs are one peak and not two; a strike near the centre of a drum head excites only its axisymmetric modes; and a contact point that lands on a mode's node silences it exactly. Reading a missing partial as a mistuned one is what made the first version of this row print five hundred cents.

The pass mark is `MODAL.md` §7.4's: every partial within **one cent**, which is roughly the threshold of pitch discrimination.

| quantity | target or published | measured | verdict | source |
|---|---|---|---|---|
| Beam: worst of the 12 partials the strike excited | within 1 cent, and no partial the physics does not predict | -0.0002 cents | meets | `MODAL.md` §7.4, the series verifier's pass mark |
| Marimba: worst of the 10 partials the strike excited | within 1 cent, and no partial the physics does not predict | +0.0002 cents | meets | `MODAL.md` §7.4, the series verifier's pass mark |
| String: worst of the 12 partials the strike excited | within 1 cent, and no partial the physics does not predict | -0.0002 cents | meets | `MODAL.md` §7.4, the series verifier's pass mark |
| Membrane: worst of the 8 partials the strike excited | within 1 cent, and no partial the physics does not predict | -0.0022 cents | meets | `MODAL.md` §7.4, the series verifier's pass mark |
| Plate: worst of the 8 partials the strike excited | within 1 cent, and no partial the physics does not predict | -0.0007 cents | meets | `MODAL.md` §7.4, the series verifier's pass mark |
| MembraneRound: worst of the 7 partials the strike excited | within 1 cent, and no partial the physics does not predict | -0.0028 cents | meets | `MODAL.md` §7.4, the series verifier's pass mark |
| Tine: worst of the 5 partials the strike excited | within 1 cent, and no partial the physics does not predict | +0.0002 cents | meets | `MODAL.md` §7.4, the series verifier's pass mark |
| PlateRound: worst of the 7 partials the strike excited | within 1 cent, and no partial the physics does not predict | +0.0002 cents | meets | `MODAL.md` §7.4, the series verifier's pass mark |

## Tuning and decay, one mode at a time

The frequency is read off the zero crossings of the mode's own output, which resolves a cent at 20 Hz where a transform's bin spacing alone is most of one. The decay is fitted to the envelope between −5 and −35 dB below its peak, which is the window `MODAL.md` §7.2 arrived at by getting it wrong twice.

The two-pole row is the comparison the formulation choice rests on. Its difference equation is van den Doel and Pai's own equation (6), written out in the benchmark rather than borrowed from the engine, so the row compares two published structures rather than this engine with itself.

| quantity | target or published | measured | verdict | source |
|---|---|---|---|---|
| a mode asked for 20 Hz rings at | within 1 cent | +0.0000 cents | meets | `MODAL.md` §7.4 |
| a mode asked for 27.5 Hz rings at | within 1 cent | +0.0002 cents | meets | `MODAL.md` §7.4 |
| a mode asked for 55 Hz rings at | within 1 cent | +0.0000 cents | meets | `MODAL.md` §7.4 |
| a mode asked for 220 Hz rings at | within 1 cent | +0.0000 cents | meets | `MODAL.md` §7.4 |
| a mode asked for 1000 Hz rings at | within 1 cent | +0.0002 cents | meets | `MODAL.md` §7.4 |
| a mode asked for 8000 Hz rings at | within 1 cent | +0.0002 cents | meets | `MODAL.md` §7.4 |
| a mode asked for 16000 Hz rings at | within 1 cent | +0.0002 cents | meets | `MODAL.md` §7.4 |
| at 20 Hz: the classic two-pole against the coupled form, both in f32 | no published figure | two-pole -8.100 cents, coupled form +0.0002 cents | — | van den Doel & Pai eq. (6) for the two-pole; `MODAL.md` §6.3 measured 6.99 cents at 20 Hz |
| at 27.5 Hz: the classic two-pole against the coupled form, both in f32 | no published figure | two-pole +2.095 cents, coupled form +0.0002 cents | — | van den Doel & Pai eq. (6) for the two-pole; `MODAL.md` §6.3 measured 6.99 cents at 20 Hz |
| at 55 Hz: the classic two-pole against the coupled form, both in f32 | no published figure | two-pole +0.371 cents, coupled form +0.0000 cents | — | van den Doel & Pai eq. (6) for the two-pole; `MODAL.md` §6.3 measured 6.99 cents at 20 Hz |
| at 440 Hz: the classic two-pole against the coupled form, both in f32 | no published figure | two-pole +0.008 cents, coupled form +0.0000 cents | — | van den Doel & Pai eq. (6) for the two-pole; `MODAL.md` §6.3 measured 6.99 cents at 20 Hz |
| a 0.5 s decay at 110 Hz measures | within 2 % | 0.5012 s (+0.24 %) | meets | `MODAL.md` §7.4, the decay checker's pass mark |
| a 2 s decay at 220 Hz measures | within 2 % | 2.0018 s (+0.09 %) | meets | `MODAL.md` §7.4, the decay checker's pass mark |
| a 3 s decay at 440 Hz measures | within 2 % | 3.0004 s (+0.01 %) | meets | `MODAL.md` §7.4, the decay checker's pass mark |
| a 8 s decay at 2000 Hz measures | within 2 % | 7.9938 s (-0.08 %) | meets | `MODAL.md` §7.4, the decay checker's pass mark |
| a 0.2 s decay at 8000 Hz measures | within 2 % | 0.2000 s (+0.00 %) | meets | `MODAL.md` §7.4, the decay checker's pass mark |

## Storing the decrement rather than the pole radius

A long decay puts the pole radius very close to 1, where `f32` has spent all its precision on the leading digit. Storing `d = 1 − r` instead puts the same number where the format still has full relative precision. Both arithmetics are written out here from their own definitions.

| quantity | target or published | measured | verdict | source |
|---|---|---|---|---|
| a 1 s decay, asked for and got | within 0.01 % | decrement 1.0000 s (+0.00000 %), radius 1.000 s (+0.01 %) | meets | `MODAL.md` §6.4, which measured 1,207 s for a wanted 1,000 |
| a 10 s decay, asked for and got | within 0.01 % | decrement 10.0000 s (+0.00000 %), radius 10.018 s (+0.18 %) | meets | `MODAL.md` §6.4, which measured 1,207 s for a wanted 1,000 |
| a 60 s decay, asked for and got | within 0.01 % | decrement 60.0000 s (-0.00001 %), radius 60.361 s (+0.60 %) | meets | `MODAL.md` §6.4, which measured 1,207 s for a wanted 1,000 |
| a 300 s decay, asked for and got | within 0.01 % | decrement 300.0000 s (+0.00000 %), radius 301.804 s (+0.60 %) | meets | `MODAL.md` §6.4, which measured 1,207 s for a wanted 1,000 |
| a 1000 s decay, asked for and got | within 0.01 % | decrement 1000.0001 s (+0.00001 %), radius 1207.218 s (+20.72 %) | meets | `MODAL.md` §6.4, which measured 1,207 s for a wanted 1,000 |

## Which modes, against a full-bank reference

The measurement the whole plug-in is about. A membrane tuned to 440 Hz has few enough partials that the **complete** bank fits in the budget, so it can be rendered as a reference; then the same object is rendered at a fixed small budget under each of the three orderings and each band is compared with the reference. The tail is off, so every decibel here belongs to the mode set.

`MODAL.md` §8.1 ran the same experiment at a 512-mode budget on a 110 Hz membrane, where the full bank is 54,749 modes, and measured a 65 dB difference between the best and the worst ordering in the 4–10 kHz band. The rows below are ours, at our own budget and our own contact positions.

| quantity | target or published | measured | verdict | source |
|---|---|---|---|---|
| the reference bank | no published figure | 3181 partials, every one the object has below 20 kHz | — | the object's own series |
| **Loudest**, 64 of 3181: highest kept, then the error against the full bank in 1–4 kHz, 4–10 kHz, 10–20 kHz | no published figure | 11205 Hz — +5.1, -1.4, -18.9 dB, worst band **18.9 dB** | — | in-house; the same experiment `MODAL.md` §8.1 ran at a different budget |
| **Lowest**, 64 of 3181: highest kept, then the error against the full bank in 1–4 kHz, 4–10 kHz, 10–20 kHz | no published figure | 2952 Hz — +8.5, -74.3, -76.6 dB, worst band **76.6 dB** | — | in-house; the same experiment `MODAL.md` §8.1 ran at a different budget |
| **Log Spread**, 64 of 3181: highest kept, then the error against the full bank in 1–4 kHz, 4–10 kHz, 10–20 kHz | no published figure | 19412 Hz — +6.1, -7.9, -8.2 dB, worst band **8.2 dB** | — | in-house; the same experiment `MODAL.md` §8.1 ran at a different budget |
| at a 64-mode budget: worst band error, best ordering against worst | no published figure | 8.2 dB against 76.6 dB — a spread of **68.4 dB** | — | in-house |
| **Loudest**, 256 of 3181: highest kept, then the error against the full bank in 1–4 kHz, 4–10 kHz, 10–20 kHz | no published figure | 11410 Hz — +5.8, -1.0, -7.0 dB, worst band **7.0 dB** | — | in-house; the same experiment `MODAL.md` §8.1 ran at a different budget |
| **Lowest**, 256 of 3181: highest kept, then the error against the full bank in 1–4 kHz, 4–10 kHz, 10–20 kHz | no published figure | 5812 Hz — +5.7, +3.8, -73.7 dB, worst band **73.7 dB** | — | in-house; the same experiment `MODAL.md` §8.1 ran at a different budget |
| **Log Spread**, 256 of 3181: highest kept, then the error against the full bank in 1–4 kHz, 4–10 kHz, 10–20 kHz | no published figure | 19854 Hz — +3.7, -2.3, -7.4 dB, worst band **7.4 dB** | — | in-house; the same experiment `MODAL.md` §8.1 ran at a different budget |
| at a 256-mode budget: worst band error, best ordering against worst | no published figure | 7.0 dB against 73.7 dB — a spread of **66.7 dB** | — | in-house |

## The air columns

An open–open column resonates at `n·c/2ℓ` and an open–closed one at `(2n−1)·c/4ℓ`, which is the whole difference between the two objects and the reason a stopped organ pipe sounds an octave below an open one of the same length. The delay is fractional and interpolated with third-order Lagrange, so the row that matters is how far the higher resonances drift from the series the terminations imply.

| quantity | target or published | measured | verdict | source |
|---|---|---|---|---|
| Tube, both ends open: worst of the first 16 resonances | within 1 cent | +0.182 cents | meets | the standard result for an air column; the pass mark is `MODAL.md` §7.4's |
| Tube, both ends open: length the model implies at 220 Hz | no published figure | 0.7550 m, round trip 4.545 ms | — | `c/2f` open and `c/4f` stopped, less Levine and Schwinger's end correction of 0.6133 a |
| Pipe, far end closed: worst of the first 16 resonances | within 1 cent | +0.527 cents | meets | the standard result for an air column; the pass mark is `MODAL.md` §7.4's |
| Pipe, far end closed: length the model implies at 220 Hz | no published figure | 0.3775 m, round trip 2.273 ms | — | `c/2f` open and `c/4f` stopped, less Levine and Schwinger's end correction of 0.6133 a |
| striking at a third of the length: every third partial, against the others | at least 25 dB apart | nulled partials -39.7 dB, the rest -1.3 dB | meets | the mode shape evaluated at the contact point; a derivation, not a control |

## The statistical tail

Above the frequency where the modal overlap factor reaches one, the partials merge into a continuum and no listener or analyser can resolve them individually. The tail covers that region with a feedback delay network whose loss filters are fitted to the same `T60(f)` the bank uses and whose level is set from the energy the selection actually left behind.

| quantity | target or published | measured | verdict | source |
|---|---|---|---|---|
| modal density of the network at 44100 Hz | above 0.15 modes/Hz | 0.364 modes/Hz | meets | Schroeder and Logan's criterion, quoted by Smith's *Physical Audio Signal Processing* |
| modal density of the network at 48000 Hz | above 0.15 modes/Hz | 0.364 modes/Hz | meets | Schroeder and Logan's criterion, quoted by Smith's *Physical Audio Signal Processing* |
| modal density of the network at 96000 Hz | above 0.15 modes/Hz | 0.364 modes/Hz | meets | Schroeder and Logan's criterion, quoted by Smith's *Physical Audio Signal Processing* |
| modal density of the network at 192000 Hz | above 0.15 modes/Hz | 0.364 modes/Hz | meets | Schroeder and Logan's criterion, quoted by Smith's *Physical Audio Signal Processing* |
| Membrane at 110 Hz, 512 modes: crossover, partials, tail level | no published figure | crossover 1165 Hz, 512 of 51673 partials modelled, tail at -20.9 dB | — | the modal overlap factor `M(f) = n(f)·B(f)`, `MODAL.md` §8.2 |
| Plate at 110 Hz, 512 modes: crossover, partials, tail level | no published figure | crossover 20000 Hz, 265 of 265 partials modelled, tail at -120.0 dB | — | the modal overlap factor `M(f) = n(f)·B(f)`, `MODAL.md` §8.2 |
| Beam at 220 Hz, 512 modes: crossover, partials, tail level | no published figure | crossover 20000 Hz, 13 of 13 partials modelled, tail at -120.0 dB | — | the modal overlap factor `M(f) = n(f)·B(f)`, `MODAL.md` §8.2 |

## Latency and the limiter

A driven modal bank has enormous gain at its own frequencies — one mode with a three-second decay is +80 dB at 440 Hz — so a limiter is not optional. Lookahead is: this one applies its gain instantly on the way down and releases slowly, which can distort a fast transient and cannot exceed the ceiling.

| quantity | target or published | measured | verdict | source |
|---|---|---|---|---|
| reported latency | zero | 0 samples | meets | ours; the device this one answers reports 64 samples unconditionally and attributes them to its own limiter |
| output peak with the ceiling at -0.3 dB and 36 dB of gain | at most -0.3 dB | -1.655 dB | meets | ours |
| output peak with the ceiling at -6 dB and 36 dB of gain | at most -6 dB | -6.000 dB | meets | ours |
| output peak with the ceiling at -12 dB and 36 dB of gain | at most -12 dB | -12.000 dB | meets | ours |

## How long the mode search takes to settle

The search for the best modes is spread across blocks with a bounded work budget, so no single block ever pays for all of it and the previous set keeps sounding meanwhile. These are the worst cases in the control's range.

| quantity | target or published | measured | verdict | source |
|---|---|---|---|---|
| Beam at 55 Hz, 28 of 28 partials | under a second | 0.003 s (1 blocks) | meets | ours; the budget is `select::WORK_PER_BLOCK` |
| String at 55 Hz, 363 of 363 partials | under a second | 0.003 s (1 blocks) | meets | ours; the budget is `select::WORK_PER_BLOCK` |
| Plate at 55 Hz, 545 of 545 partials | under a second | 0.003 s (1 blocks) | meets | ours; the budget is `select::WORK_PER_BLOCK` |
| Membrane at 110 Hz, 4096 of 51673 partials | under a second | 0.019 s (7 blocks) | meets | ours; the budget is `select::WORK_PER_BLOCK` |
| Membrane at 20 Hz, 4096 of 1569392 partials | under a second | 0.512 s (192 blocks) | meets | ours; the budget is `select::WORK_PER_BLOCK` |
| MembraneRound at 55 Hz, 4096 of 16384 partials | under a second | 0.045 s (17 blocks) | meets | ours; the budget is `select::WORK_PER_BLOCK` |
| Tine at 55 Hz, 11 of 11 partials | under a second | 0.003 s (1 blocks) | meets | ours; the budget is `select::WORK_PER_BLOCK` |
| PlateRound at 55 Hz, 421 of 421 partials | under a second | 0.003 s (1 blocks) | meets | ours; the budget is `select::WORK_PER_BLOCK` |
| cost of one block while the search is running, worst case in the range | under a quarter of the block period | 0.459 ms against a 2.667 ms block — 17.2 % of it | meets | ours; this is what `select::WORK_PER_BLOCK` is chosen against |

## What a mode costs

Timed on this machine, in a debug or release build depending on how the binary was run — the numbers below are only meaningful from `cargo run --release`. Every figure is the best of five runs, because a busy machine can only make a measurement worse.

**A mode's cost is a rate, not a total.** The percentage columns are that rate against one core at 48 kHz, so they say how much of a core a voice of that size takes.

| quantity | target or published | measured | verdict | source |
|---|---|---|---|---|
| 256 modes, stereo, block 128: the decrement kept separate | no published figure | 0.316 ns/mode/sample — 0.39 % of one core at 48 kHz | — | in-house |
| 256 modes: what those two extra multiplies cost | no published figure | 0.240 ns folded against 0.316 ns split, +31.9 % | — | in-house; the accuracy they buy is in the decrement section above |
| 1024 modes, stereo, block 128: the decrement kept separate | no published figure | 0.351 ns/mode/sample — 1.73 % of one core at 48 kHz | — | in-house |
| 1024 modes: what those two extra multiplies cost | no published figure | 0.286 ns folded against 0.351 ns split, +22.7 % | — | in-house; the accuracy they buy is in the decrement section above |
| 4096 modes, stereo, block 128: the decrement kept separate | no published figure | 0.362 ns/mode/sample — 7.13 % of one core at 48 kHz | — | in-house |
| 4096 modes: what those two extra multiplies cost | no published figure | 0.278 ns folded against 0.362 ns split, +30.4 % | — | in-house; the accuracy they buy is in the decrement section above |
| 1,024 modes at block 1 | no published figure | 1.152 ns/mode/sample | — | in-house; `MODAL.md` §4.2 measured per-sample processing at 8× the cost of block 128 |
| 1,024 modes at block 4 | no published figure | 0.567 ns/mode/sample | — | in-house; `MODAL.md` §4.2 measured per-sample processing at 8× the cost of block 128 |
| 1,024 modes at block 16 | no published figure | 0.398 ns/mode/sample | — | in-house; `MODAL.md` §4.2 measured per-sample processing at 8× the cost of block 128 |
| 1,024 modes at block 64 | no published figure | 0.360 ns/mode/sample | — | in-house; `MODAL.md` §4.2 measured per-sample processing at 8× the cost of block 128 |
| 1,024 modes at block 128 | no published figure | 0.356 ns/mode/sample | — | in-house; `MODAL.md` §4.2 measured per-sample processing at 8× the cost of block 128 |
| 1,024 modes at block 256 | no published figure | 0.362 ns/mode/sample | — | in-house; `MODAL.md` §4.2 measured per-sample processing at 8× the cost of block 128 |
| an air column at 55 Hz, 64 resonances published, stereo | no published figure | 35.687 ns/sample — 0.17 % of one core at 48 kHz, whatever number of harmonics come out | — | in-house; a delay loop costs its index arithmetic and its taps, not its partials |
| how many partials a mode bank can afford for the price of the whole air column | no published figure | 96 partials | — | in-house. `MODAL.md`'s own loop broke even at about twelve, and the difference is              the contact combs: this one carries six fractional taps per sample — two reflections,              two injections and two pickups — where a bare loop carries one |
| and why that decides nothing on its own | no published figure | a bar has 28 partials and an inharmonic series, so no waveguide can represent it at any price;              an air column has hundreds and they are exactly harmonic, so no bank should have to | — | the two series, from `src/dsp/object.rs` and `src/dsp/guide.rs` |
| the whole device, 256 modes on a membrane | no published figure | 0.503 ns/mode/sample — 0.62 % of one core at 48 kHz, tail and limiter included | — | in-house |
| the whole device, 1024 modes on a membrane | no published figure | 0.371 ns/mode/sample — 1.82 % of one core at 48 kHz, tail and limiter included | — | in-house |
| the whole device, 4096 modes on a membrane | no published figure | 0.382 ns/mode/sample — 7.51 % of one core at 48 kHz, tail and limiter included | — | in-house |
| 1024 modes with the pitch oscillator off | no published figure | 0.396 ns/mode/sample | — | in-house; the published trick that makes a single resonator's retune free does not transfer to a bank, because every mode turns by a different angle |
| 1024 modes with the pitch oscillator **on** | no published figure | 2.098 ns/mode/sample | — | in-house; the published trick that makes a single resonator's retune free does not transfer to a bank, because every mode turns by a different angle |

## The out-of-tree probe

`tools/physics_probe.py` computes every partial series again from the published formulae, in a language that cannot link against this one. Bessel functions come from the integral representation `J_m(x) = (1/π)∫₀^π cos(mτ − x sin τ)dτ`, beam eigenvalues from bisection on `cos β − sech β = 0`, and the membrane and plate lattices from their closed forms. It checks itself against Leissa's Table 4.23, Abramowitz and Stegun's Table 9.5, Russell's circular-membrane ratios and Lehtonen's inharmonicity figures **before** it is used on anything of ours, and then diffs `benchmark --dump` against its own arithmetic.

Run it with `python tools/physics_probe.py --compare series.csv`. The worst disagreement it has found is **0.0001 cents**, across every object and every partial the two both cover.

## What is missed, and why

Nothing in this file is a widened tolerance. Where a row misses, the row stays and the miss is named.

Three limits are structural rather than measured, and none of them is a defect to be fixed later:

* **The tail does not match the modal density law and cannot.** A feedback delay network's density is constant with frequency where a membrane's rises linearly. Above the resolvability crossover that difference is inaudible by construction — the requirement up there is that the density *exceed* what the ear can resolve, not that it take a particular value — and the network clears Schroeder and Logan's criterion across the band by more than a factor of two.
* **The plate is the simply supported one.** A struck plate is physically free on all four edges and the free rectangular plate has no closed form; Leissa gives Ritz-method tables and nothing else. The case that can be solved is solved, and which case it is is stated rather than glossed.
* **The round membrane's exact-mode region is capped by its zero table.** Beyond it the partials sit far above any tuning's crossover, where the tail is the right model and individual modes are not.

And one honest boundary that is a design decision rather than a limit: **the air columns do not blow.** A real wind instrument is a nonlinear exciter in a feedback loop with its bore, and this is a passive linear resonator driven by whatever audio is put into it. It rings like a tapped length of pipe because that is what it is.
